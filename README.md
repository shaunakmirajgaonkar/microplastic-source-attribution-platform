# 🧪 MicroSource Local

Local-first river microplastic source-screening dashboard.

## Run
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m streamlit run app.py
```

Included data is synthetic demonstration data. Scores are screening signals and do not establish source attribution.
